import { JacClient } from 'jac-client';

// Note: If you are using an Android Emulator, use 'http://10.0.2.2:8000' instead of localhost
const jac = new JacClient('http://localhost:8000');

export default jac;
